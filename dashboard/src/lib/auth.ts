/**
 * Shared auth constant for dashboard password gate.
 * Architecture: DEPLOYMENT_ARCHITECTURE.md §9
 * Used by: App.tsx (ProtectedDashboard guard), pages/Login.tsx (setter)
 */
export const AUTH_KEY = 'llm_tester_auth'
