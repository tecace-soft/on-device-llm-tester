# Vercel environment variable template
# Set VITE_API_URL in Vercel Dashboard → Project Settings → Environment Variables
# Do NOT commit .env.production — add the value directly in Vercel UI instead.
VITE_API_URL=https://llm-tester-api.onrender.com
